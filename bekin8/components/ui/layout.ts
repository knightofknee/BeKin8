// components/ui/layout.ts
// Screen-level horizontal padding — the "dead space" on each side of content.
// Used by all main app screens. Login/signup and the navigation bar are exceptions.
export const SCREEN_PAD = 10;
